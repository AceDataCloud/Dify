apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: platform-service-dify-web
  name: platform-service-dify-web
  namespace: acedatacloud
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: platform-service-dify-web
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: platform-service-dify-web
    spec:
      automountServiceAccountToken: false
      containers:
      - env:
        - name: CONSOLE_API_URL
        - name: APP_API_URL
        - name: APP_WEB_URL
        image: ghcr.io/acedatacloud/platform-service-dify-web:latest
        imagePullPolicy: IfNotPresent
        name: platform-service-dify-web
        ports:
        - containerPort: 3000
          protocol: TCP
        resources:
          limits:
            cpu: 200m
            memory: 1Gi
          requests:
            cpu: 20m
            memory: 128Mi
        securityContext:
          privileged: false
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      imagePullSecrets:
      - name: docker-registry
      nodeSelector:
        kubernetes.io/os: linux
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
