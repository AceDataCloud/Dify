apiVersion: v1
kind: Service
metadata:
  name: platform-service-dify-nginx
  namespace: acedatacloud
spec:
  type: ClusterIP
  selector:
    app: platform-service-dify-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
