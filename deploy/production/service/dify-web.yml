apiVersion: v1
kind: Service
metadata:
  name: platform-service-dify-web
  namespace: acedatacloud
spec:
  type: ClusterIP
  selector:
    app: platform-service-dify-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
