apiVersion: v1
kind: Service
metadata:
  name: dify-plugin-daemon
  namespace: acedatacloud
spec:
  type: ClusterIP
  selector:
    app: dify-plugin-daemon
  ports:
    - name: http
      port: 5002
      targetPort: 5002
