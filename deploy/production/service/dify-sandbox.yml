apiVersion: v1
kind: Service
metadata:
  name: platform-service-dify-sandbox
  namespace: acedatacloud
spec:
  type: ClusterIP
  selector:
    app: platform-service-dify-sandbox
  ports:
    - name: http
      port: 8194
      targetPort: 8194
